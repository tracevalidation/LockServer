CONSTANTS
    (* Set constants specific to algorithm *)
    Nil <- TraceNil
    Clients <- TraceClients
    Server <- TraceServer
    (* Set default action for TVOperators module *)
    \* Default <- locksvDefault
    (* Set variables of TraceSpec module *)
    Vars <- vars
    (* Set operator for TraceSpec module *)
    BaseInit <- LOCKsvInit
    UpdateVariables <- LOCKsvUpdateVariables
    TraceNext <- LOCKsvTraceNext

SPECIFICATION
    TraceSpec

PROPERTIES
    BaseSpec

VIEW
    TraceView

CONSTRAINT 
    Termination

POSTCONDITION
    TraceAccepted

CHECK_DEADLOCK
    FALSE